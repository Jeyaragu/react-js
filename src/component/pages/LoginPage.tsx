import LoginLayout from '../templates/LoginLayout'

// This is the router entry point for the login page. It uses the LoginLayout template to render the login page. It follows Login business flow.
const LoginPage = () => <LoginLayout />;

export default LoginPage;